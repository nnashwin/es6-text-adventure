{
  "name": "text-adventure",
  "version": "1.0.0",
  "description": "",
  "main": "first.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "clean": "nodemon --exec 'rm -rf ./output/*'",
    "compile": "nodemon --ignore output/ --exec 'npm run script'",
    "script": "rm -rf output/* && babel --stage 0 src/* >> output/build.js"
  },
  "author": "",
  "standard": {
    "parser": "babel-eslint"
  },
  "license": "ISC"
}
